---
title: 'API Call Example'
description: 'See a real Pointer API call and its streaming response.'
---

## Example: Streaming API Call

The Agentic API streams responses by default using Server-Sent Events (SSE). This allows you to receive real-time updates as the agent thinks and acts.

### Request
```bash
curl -X POST \
  -H "Content-Type: application/json" \
  -H "Accept: text/event-stream" \
  -H "Authorization: Bearer sk-ptr-..." \
  -d '{"message": "Can you introduce yourself?"}' \
  "https://pointerapi.com"
```

### Streaming Response
```text
data: {"type":"agent_started","session":"8lh87hwhju0xoxy"}
... (truncated)
```

## Session Handling
- **Auto-Creation**: If you do not specify a session ID, a new one is automatically created.
- **Continue a Session**: Append `?session=[SESSION_ID]` to the URL. 